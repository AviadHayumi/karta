---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: kartas.run.ai
spec:
  group: run.ai
  names:
    kind: Karta
    listKind: KartaList
    plural: kartas
    shortNames:
    - krt
    singular: karta
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - description: Target framework kind
      jsonPath: .spec.structureDefinition.rootComponent.kind.kind
      name: Framework
      type: string
    - description: Root component name
      jsonPath: .spec.structureDefinition.rootComponent.name
      name: Root Component
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Karta
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              optimizationInstructions:
                description: Instructions contains optimization-specific instructions
                  for the workload
                properties:
                  gangScheduling:
                    properties:
                      podGroup:
                        description: PodGroupComponentsMapping defines the grouping,
                          subgroup, and topology behavior.
                        properties:
                          name:
                            description: Name is the unique identifier for this pod
                              group.
                            type: string
                          subGroups:
                            description: SubGroups defines which Karta components
                              should become subGroups.
                            items:
                              properties:
                                componentName:
                                  description: ComponentName references a component
                                    defined in the Karta structure.
                                  type: string
                                topology:
                                  description: Topology defines the topology constraint
                                    for this component's pods.
                                  properties:
                                    preferredTopologyLevel:
                                      description: PreferredTopologyLevel is the preferred
                                        locality level.
                                      type: string
                                    requiredTopologyLevel:
                                      description: |-
                                        RequiredTopologyLevel is the maximal level that all matching pods must
                                        fit within.
                                      type: string
                                    topologyName:
                                      description: TopologyName is the topology resource
                                        used by the constraint.
                                      type: string
                                  required:
                                  - preferredTopologyLevel
                                  - requiredTopologyLevel
                                  - topologyName
                                  type: object
                              required:
                              - componentName
                              type: object
                            type: array
                            x-kubernetes-list-map-keys:
                            - componentName
                            x-kubernetes-list-type: map
                          topology:
                            description: Topology defines the topology constraint
                              for all workload pods.
                            properties:
                              preferredTopologyLevel:
                                description: PreferredTopologyLevel is the preferred
                                  locality level.
                                type: string
                              requiredTopologyLevel:
                                description: |-
                                  RequiredTopologyLevel is the maximal level that all matching pods must
                                  fit within.
                                type: string
                              topologyName:
                                description: TopologyName is the topology resource
                                  used by the constraint.
                                type: string
                            required:
                            - preferredTopologyLevel
                            - requiredTopologyLevel
                            - topologyName
                            type: object
                        required:
                        - name
                        type: object
                      podGroups:
                        description: |-
                          PodGroups defines the alpha grouping format.
                          This is deprecated and will be removed in a future release.
                          Deprecated: Please use gangScheduling.podGroup  instead.
                        items:
                          description: |-
                            PodGroupDefinition defines the alpha grouping format.
                            This is deprecated and will be removed in a future release.
                          properties:
                            members:
                              description: Members defines which components belong
                                to this pod group.
                              items:
                                description: |-
                                  PodGroupMemberDefinition defines how to select and filter components for grouping instructions.
                                  This is deprecated and will be removed in a future release.
                                properties:
                                  componentName:
                                    description: ComponentName references a component
                                      defined in the Karta structure.
                                    type: string
                                  groupByExpressions:
                                    description: |-
                                      GroupByExpressions are CEL expressions evaluated against individual pod objects, each
                                      returning a single grouping value. If empty, grouping is done via owner reference traversal.
                                    items:
                                      type: string
                                    type: array
                                    x-kubernetes-list-type: set
                                required:
                                - componentName
                                type: object
                              type: array
                              x-kubernetes-list-map-keys:
                              - componentName
                              x-kubernetes-list-type: map
                            name:
                              description: Name is the unique identifier for this
                                pod group.
                              type: string
                          required:
                          - members
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                    type: object
                type: object
              structureDefinition:
                description: StructureDefinition defines the compute hierarchy and
                  component relationships
                properties:
                  additionalChildKinds:
                    description: |-
                      AdditionalChildKinds lists Kubernetes kinds that are created/managed by this workload
                      but are not explicitly modeled as components (e.g., Deployments, Services).
                      Required for RBAC purposes, etc.
                    items:
                      description: GroupVersionKind represents a Kubernetes API object's
                        group, version, and kind.
                      properties:
                        group:
                          description: Group is the API group of the resource
                          type: string
                        kind:
                          description: Kind is the API kind of the resource
                          type: string
                        version:
                          description: Version is the API version of the resource
                          type: string
                      required:
                      - group
                      - kind
                      - version
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - kind
                    x-kubernetes-list-type: map
                  childComponents:
                    description: ChildComponents defines the child components in the
                      hierarchy
                    items:
                      description: |-
                        ComponentDefinition defines a single component in the workload hierarchy.
                        Components represent logical units of computation that can be optimized independently.
                      properties:
                        fields:
                          additionalProperties:
                            description: |-
                              ValueAccessor reads and locates one workload field.
                              Expression is evaluated by the definition's engine and returns the field's value.
                              PathWrite or PathWriteExpression locates the field for writes. An accessor without
                              either is read-only. The SDK owns patch construction and write strategy.
                            properties:
                              expression:
                                description: Expression returns the field's value.
                                type: string
                              pathWrite:
                                description: PathWrite is the field's RFC 6901 JSON
                                  Pointer. An empty string targets the document root.
                                pattern: ^(/([^~/]|~[01])*)*$
                                type: string
                              pathWriteExpression:
                                description: PathWriteExpression returns the field's
                                  RFC 6901 JSON Pointer using the definition's engine.
                                minLength: 1
                                type: string
                            type: object
                            x-kubernetes-validations:
                            - message: pathWrite and pathWriteExpression are mutually
                                exclusive
                              rule: '!(has(self.pathWrite) && has(self.pathWriteExpression))'
                          description: |-
                            Fields exposes custom named workload values through the SDK's field accessors.
                            Built-in SDK field names are reserved.
                          type: object
                        instanceIds:
                          description: InstanceIds is an expression returning the
                            list of instance ids.
                          properties:
                            expression:
                              description: Expression returns the field's value.
                              type: string
                            pathWrite:
                              description: PathWrite is the field's RFC 6901 JSON
                                Pointer. An empty string targets the document root.
                              pattern: ^(/([^~/]|~[01])*)*$
                              type: string
                            pathWriteExpression:
                              description: PathWriteExpression returns the field's
                                RFC 6901 JSON Pointer using the definition's engine.
                              minLength: 1
                              type: string
                          type: object
                          x-kubernetes-validations:
                          - message: instanceIds is read-only
                            rule: '!has(self.pathWrite) && !has(self.pathWriteExpression)'
                          - message: pathWrite and pathWriteExpression are mutually
                              exclusive
                            rule: '!(has(self.pathWrite) && has(self.pathWriteExpression))'
                        kind:
                          description: Kind specifies the Kubernetes GroupVersionKind
                            for this component
                          properties:
                            group:
                              description: Group is the API group of the resource
                              type: string
                            kind:
                              description: Kind is the API kind of the resource
                              type: string
                            version:
                              description: Version is the API version of the resource
                              type: string
                          required:
                          - group
                          - kind
                          - version
                          type: object
                        name:
                          description: Name is the unique identifier for this component
                            within the Karta
                          type: string
                        ownerRef:
                          description: OwnerRef references the parent component in
                            the hierarchy by name (nil for root component)
                          type: string
                        podSelector:
                          description: PodSelector defines how to identify pods belonging
                            to this component
                          properties:
                            componentInstanceSelector:
                              description: |-
                                ComponentInstanceSelector splits a single ComponentDefinition into multiple
                                component instances, each identified by a unique value extracted from the pod.
                                Use this when instances of the same component type represent fundamentally
                                different roles or services (e.g., Dynamo "service" definition produces
                                separate "Frontend", "PrefillWorker", "DecodeWorker" instances).
                                This is distinct from ReplicaSelector: ComponentInstanceSelector creates
                                instances with potentially different specs/behavior, while ReplicaSelector
                                creates replicas of an identical sub-structure.
                              properties:
                                expression:
                                  description: Expression reads and writes this field.
                                  type: string
                              type: object
                            componentTypeSelector:
                              description: |-
                                ComponentTypeSelector determines whether a pod belongs to this component type
                                by matching a pod label or annotation value via an expression.
                                This is the primary mechanism for pod-to-component membership.
                                For example, LWS "leader" uses worker-index="0" to identify leader pods,
                                and LWS "worker" checks for the existence of the leader-name annotation.
                              properties:
                                expression:
                                  description: Expression reads and writes this field.
                                  type: string
                                value:
                                  description: Value is the expected value for the
                                    key (optional - if nil, only key existence is
                                    checked)
                                  type: string
                              type: object
                            replicaSelector:
                              description: |-
                                ReplicaSelector identifies which replica index or group the pod belongs to.
                                Use this to distinguish between multiple replicas of the same component / component instance
                                sub-structure (e.g., LWS Group 0 vs Group 1, where each group has identical
                                leader/worker descendants). Descendant components automatically inherit the replica
                                context from their ancestor (parent), so ReplicaSelector should only be defined at
                                the level where replicas are created, not repeated in descendants.
                              properties:
                                expression:
                                  description: Expression reads and writes this field.
                                  type: string
                              type: object
                          type: object
                        scaleDefinition:
                          description: ScaleDefinition defines how to extract scaling
                            information from this component
                          properties:
                            maxReplicas:
                              description: MaxReplicas reads and writes this field.
                              properties:
                                expression:
                                  description: Expression returns the field's value.
                                  type: string
                                pathWrite:
                                  description: PathWrite is the field's RFC 6901 JSON
                                    Pointer. An empty string targets the document
                                    root.
                                  pattern: ^(/([^~/]|~[01])*)*$
                                  type: string
                                pathWriteExpression:
                                  description: PathWriteExpression returns the field's
                                    RFC 6901 JSON Pointer using the definition's engine.
                                  minLength: 1
                                  type: string
                              type: object
                              x-kubernetes-validations:
                              - message: pathWrite and pathWriteExpression are mutually
                                  exclusive
                                rule: '!(has(self.pathWrite) && has(self.pathWriteExpression))'
                            minReplicas:
                              description: MinReplicas reads and writes this field.
                              properties:
                                expression:
                                  description: Expression returns the field's value.
                                  type: string
                                pathWrite:
                                  description: PathWrite is the field's RFC 6901 JSON
                                    Pointer. An empty string targets the document
                                    root.
                                  pattern: ^(/([^~/]|~[01])*)*$
                                  type: string
                                pathWriteExpression:
                                  description: PathWriteExpression returns the field's
                                    RFC 6901 JSON Pointer using the definition's engine.
                                  minLength: 1
                                  type: string
                              type: object
                              x-kubernetes-validations:
                              - message: pathWrite and pathWriteExpression are mutually
                                  exclusive
                                rule: '!(has(self.pathWrite) && has(self.pathWriteExpression))'
                            replicas:
                              description: Replicas reads and writes this field.
                              properties:
                                expression:
                                  description: Expression returns the field's value.
                                  type: string
                                pathWrite:
                                  description: PathWrite is the field's RFC 6901 JSON
                                    Pointer. An empty string targets the document
                                    root.
                                  pattern: ^(/([^~/]|~[01])*)*$
                                  type: string
                                pathWriteExpression:
                                  description: PathWriteExpression returns the field's
                                    RFC 6901 JSON Pointer using the definition's engine.
                                  minLength: 1
                                  type: string
                              type: object
                              x-kubernetes-validations:
                              - message: pathWrite and pathWriteExpression are mutually
                                  exclusive
                                rule: '!(has(self.pathWrite) && has(self.pathWriteExpression))'
                          type: object
                        specDefinition:
                          description: SpecDefinition defines how to extract pod specifications
                            from this component
                          properties:
                            fragmentedPodSpecDefinition:
                              description: |-
                                FragmentedPodSpecDefinition defines how to extract individual pod spec fields
                                when they are scattered across different paths in the component
                              properties:
                                annotations:
                                  description: Annotations reads and writes this field.
                                  properties:
                                    expression:
                                      description: Expression returns the field's
                                        value.
                                      type: string
                                    pathWrite:
                                      description: PathWrite is the field's RFC 6901
                                        JSON Pointer. An empty string targets the
                                        document root.
                                      pattern: ^(/([^~/]|~[01])*)*$
                                      type: string
                                    pathWriteExpression:
                                      description: PathWriteExpression returns the
                                        field's RFC 6901 JSON Pointer using the definition's
                                        engine.
                                      minLength: 1
                                      type: string
                                  type: object
                                  x-kubernetes-validations:
                                  - message: pathWrite and pathWriteExpression are
                                      mutually exclusive
                                    rule: '!(has(self.pathWrite) && has(self.pathWriteExpression))'
                                container:
                                  description: Container reads and writes this field.
                                  properties:
                                    expression:
                                      description: Expression returns the field's
                                        value.
                                      type: string
                                    pathWrite:
                                      description: PathWrite is the field's RFC 6901
                                        JSON Pointer. An empty string targets the
                                        document root.
                                      pattern: ^(/([^~/]|~[01])*)*$
                                      type: string
                                    pathWriteExpression:
                                      description: PathWriteExpression returns the
                                        field's RFC 6901 JSON Pointer using the definition's
                                        engine.
                                      minLength: 1
                                      type: string
                                  type: object
                                  x-kubernetes-validations:
                                  - message: pathWrite and pathWriteExpression are
                                      mutually exclusive
                                    rule: '!(has(self.pathWrite) && has(self.pathWriteExpression))'
                                containers:
                                  description: Containers reads and writes this field.
                                  properties:
                                    expression:
                                      description: Expression returns the field's
                                        value.
                                      type: string
                                    pathWrite:
                                      description: PathWrite is the field's RFC 6901
                                        JSON Pointer. An empty string targets the
                                        document root.
                                      pattern: ^(/([^~/]|~[01])*)*$
                                      type: string
                                    pathWriteExpression:
                                      description: PathWriteExpression returns the
                                        field's RFC 6901 JSON Pointer using the definition's
                                        engine.
                                      minLength: 1
                                      type: string
                                  type: object
                                  x-kubernetes-validations:
                                  - message: pathWrite and pathWriteExpression are
                                      mutually exclusive
                                    rule: '!(has(self.pathWrite) && has(self.pathWriteExpression))'
                                image:
                                  description: Image reads and writes this field.
                                  properties:
                                    expression:
                                      description: Expression returns the field's
                                        value.
                                      type: string
                                    pathWrite:
                                      description: PathWrite is the field's RFC 6901
                                        JSON Pointer. An empty string targets the
                                        document root.
                                      pattern: ^(/([^~/]|~[01])*)*$
                                      type: string
                                    pathWriteExpression:
                                      description: PathWriteExpression returns the
                                        field's RFC 6901 JSON Pointer using the definition's
                                        engine.
                                      minLength: 1
                                      type: string
                                  type: object
                                  x-kubernetes-validations:
                                  - message: pathWrite and pathWriteExpression are
                                      mutually exclusive
                                    rule: '!(has(self.pathWrite) && has(self.pathWriteExpression))'
                                labels:
                                  description: Labels reads and writes this field.
                                  properties:
                                    expression:
                                      description: Expression returns the field's
                                        value.
                                      type: string
                                    pathWrite:
                                      description: PathWrite is the field's RFC 6901
                                        JSON Pointer. An empty string targets the
                                        document root.
                                      pattern: ^(/([^~/]|~[01])*)*$
                                      type: string
                                    pathWriteExpression:
                                      description: PathWriteExpression returns the
                                        field's RFC 6901 JSON Pointer using the definition's
                                        engine.
                                      minLength: 1
                                      type: string
                                  type: object
                                  x-kubernetes-validations:
                                  - message: pathWrite and pathWriteExpression are
                                      mutually exclusive
                                    rule: '!(has(self.pathWrite) && has(self.pathWriteExpression))'
                                nodeAffinity:
                                  description: NodeAffinity reads and writes this
                                    field.
                                  properties:
                                    expression:
                                      description: Expression returns the field's
                                        value.
                                      type: string
                                    pathWrite:
                                      description: PathWrite is the field's RFC 6901
                                        JSON Pointer. An empty string targets the
                                        document root.
                                      pattern: ^(/([^~/]|~[01])*)*$
                                      type: string
                                    pathWriteExpression:
                                      description: PathWriteExpression returns the
                                        field's RFC 6901 JSON Pointer using the definition's
                                        engine.
                                      minLength: 1
                                      type: string
                                  type: object
                                  x-kubernetes-validations:
                                  - message: pathWrite and pathWriteExpression are
                                      mutually exclusive
                                    rule: '!(has(self.pathWrite) && has(self.pathWriteExpression))'
                                podAffinity:
                                  description: PodAffinity reads and writes this field.
                                  properties:
                                    expression:
                                      description: Expression returns the field's
                                        value.
                                      type: string
                                    pathWrite:
                                      description: PathWrite is the field's RFC 6901
                                        JSON Pointer. An empty string targets the
                                        document root.
                                      pattern: ^(/([^~/]|~[01])*)*$
                                      type: string
                                    pathWriteExpression:
                                      description: PathWriteExpression returns the
                                        field's RFC 6901 JSON Pointer using the definition's
                                        engine.
                                      minLength: 1
                                      type: string
                                  type: object
                                  x-kubernetes-validations:
                                  - message: pathWrite and pathWriteExpression are
                                      mutually exclusive
                                    rule: '!(has(self.pathWrite) && has(self.pathWriteExpression))'
                                priorityClassName:
                                  description: PriorityClassName reads and writes
                                    this field.
                                  properties:
                                    expression:
                                      description: Expression returns the field's
                                        value.
                                      type: string
                                    pathWrite:
                                      description: PathWrite is the field's RFC 6901
                                        JSON Pointer. An empty string targets the
                                        document root.
                                      pattern: ^(/([^~/]|~[01])*)*$
                                      type: string
                                    pathWriteExpression:
                                      description: PathWriteExpression returns the
                                        field's RFC 6901 JSON Pointer using the definition's
                                        engine.
                                      minLength: 1
                                      type: string
                                  type: object
                                  x-kubernetes-validations:
                                  - message: pathWrite and pathWriteExpression are
                                      mutually exclusive
                                    rule: '!(has(self.pathWrite) && has(self.pathWriteExpression))'
                                resourceClaims:
                                  description: ResourceClaims reads and writes this
                                    field.
                                  properties:
                                    expression:
                                      description: Expression returns the field's
                                        value.
                                      type: string
                                    pathWrite:
                                      description: PathWrite is the field's RFC 6901
                                        JSON Pointer. An empty string targets the
                                        document root.
                                      pattern: ^(/([^~/]|~[01])*)*$
                                      type: string
                                    pathWriteExpression:
                                      description: PathWriteExpression returns the
                                        field's RFC 6901 JSON Pointer using the definition's
                                        engine.
                                      minLength: 1
                                      type: string
                                  type: object
                                  x-kubernetes-validations:
                                  - message: pathWrite and pathWriteExpression are
                                      mutually exclusive
                                    rule: '!(has(self.pathWrite) && has(self.pathWriteExpression))'
                                resources:
                                  description: Resources reads and writes this field.
                                  properties:
                                    expression:
                                      description: Expression returns the field's
                                        value.
                                      type: string
                                    pathWrite:
                                      description: PathWrite is the field's RFC 6901
                                        JSON Pointer. An empty string targets the
                                        document root.
                                      pattern: ^(/([^~/]|~[01])*)*$
                                      type: string
                                    pathWriteExpression:
                                      description: PathWriteExpression returns the
                                        field's RFC 6901 JSON Pointer using the definition's
                                        engine.
                                      minLength: 1
                                      type: string
                                  type: object
                                  x-kubernetes-validations:
                                  - message: pathWrite and pathWriteExpression are
                                      mutually exclusive
                                    rule: '!(has(self.pathWrite) && has(self.pathWriteExpression))'
                                schedulerName:
                                  description: SchedulerName reads and writes this
                                    field.
                                  properties:
                                    expression:
                                      description: Expression returns the field's
                                        value.
                                      type: string
                                    pathWrite:
                                      description: PathWrite is the field's RFC 6901
                                        JSON Pointer. An empty string targets the
                                        document root.
                                      pattern: ^(/([^~/]|~[01])*)*$
                                      type: string
                                    pathWriteExpression:
                                      description: PathWriteExpression returns the
                                        field's RFC 6901 JSON Pointer using the definition's
                                        engine.
                                      minLength: 1
                                      type: string
                                  type: object
                                  x-kubernetes-validations:
                                  - message: pathWrite and pathWriteExpression are
                                      mutually exclusive
                                    rule: '!(has(self.pathWrite) && has(self.pathWriteExpression))'
                              type: object
                            metadata:
                              description: Metadata reads and writes this field.
                              properties:
                                expression:
                                  description: Expression returns the field's value.
                                  type: string
                                pathWrite:
                                  description: PathWrite is the field's RFC 6901 JSON
                                    Pointer. An empty string targets the document
                                    root.
                                  pattern: ^(/([^~/]|~[01])*)*$
                                  type: string
                                pathWriteExpression:
                                  description: PathWriteExpression returns the field's
                                    RFC 6901 JSON Pointer using the definition's engine.
                                  minLength: 1
                                  type: string
                              type: object
                              x-kubernetes-validations:
                              - message: pathWrite and pathWriteExpression are mutually
                                  exclusive
                                rule: '!(has(self.pathWrite) && has(self.pathWriteExpression))'
                            podSpec:
                              description: PodSpec reads and writes the complete PodSpec
                                object.
                              properties:
                                expression:
                                  description: Expression returns the field's value.
                                  type: string
                                pathWrite:
                                  description: PathWrite is the field's RFC 6901 JSON
                                    Pointer. An empty string targets the document
                                    root.
                                  pattern: ^(/([^~/]|~[01])*)*$
                                  type: string
                                pathWriteExpression:
                                  description: PathWriteExpression returns the field's
                                    RFC 6901 JSON Pointer using the definition's engine.
                                  minLength: 1
                                  type: string
                              type: object
                              x-kubernetes-validations:
                              - message: pathWrite and pathWriteExpression are mutually
                                  exclusive
                                rule: '!(has(self.pathWrite) && has(self.pathWriteExpression))'
                            podTemplateSpec:
                              description: PodTemplateSpec reads and writes the complete
                                PodTemplateSpec object.
                              properties:
                                expression:
                                  description: Expression returns the field's value.
                                  type: string
                                pathWrite:
                                  description: PathWrite is the field's RFC 6901 JSON
                                    Pointer. An empty string targets the document
                                    root.
                                  pattern: ^(/([^~/]|~[01])*)*$
                                  type: string
                                pathWriteExpression:
                                  description: PathWriteExpression returns the field's
                                    RFC 6901 JSON Pointer using the definition's engine.
                                  minLength: 1
                                  type: string
                              type: object
                              x-kubernetes-validations:
                              - message: pathWrite and pathWriteExpression are mutually
                                  exclusive
                                rule: '!(has(self.pathWrite) && has(self.pathWriteExpression))'
                          type: object
                        statusDefinition:
                          description: |-
                            StatusDefinition defines how to interpret the status of this component
                            should be added only for the root component
                          properties:
                            conditionsDefinition:
                              description: ConditionsDefinition defines how to extract
                                Kubernetes-style conditions
                              properties:
                                expression:
                                  description: Expression reads and writes this field.
                                  type: string
                                messageFieldName:
                                  default: message
                                  description: MessageFieldName is the field name
                                    for the condition text message
                                  type: string
                                reasonFieldName:
                                  default: reason
                                  description: ReasonFieldName is the field name for
                                    the condition reason
                                  type: string
                                statusFieldName:
                                  default: status
                                  description: StatusFieldName is the field name for
                                    the condition status
                                  type: string
                                typeFieldName:
                                  default: type
                                  description: TypeFieldName is the field name for
                                    the condition type
                                  type: string
                              type: object
                            phaseDefinition:
                              description: PhaseDefinition defines how to extract
                                a simple phase/state string
                              properties:
                                expression:
                                  description: Expression reads and writes this field.
                                  type: string
                              type: object
                            statusMappings:
                              description: StatusMappings define how to map extracted
                                status to ResourceStatus values
                              properties:
                                completed:
                                  description: |-
                                    Completed defines matchers for the Completed status.
                                    Multiple matchers are OR'd together.
                                  items:
                                    description: |-
                                      StatusMatcher defines criteria for matching a specific status.
                                      If both ByPhase and ByConditions are provided, ALL must match (AND logic).
                                    properties:
                                      byConditions:
                                        description: ByConditions matches against
                                          specific condition combinations (ANDed together)
                                          at least one of status or reason must be
                                          used.
                                        items:
                                          description: ExpectedCondition defines a
                                            condition type and status that must be
                                            present.
                                          properties:
                                            reason:
                                              description: Reason is the expected
                                                condition reason
                                              type: string
                                            status:
                                              description: Status is the expected
                                                condition status
                                              type: string
                                            type:
                                              description: Type is the condition type
                                                to match
                                              type: string
                                          required:
                                          - type
                                          type: object
                                        type: array
                                        x-kubernetes-list-type: atomic
                                      byExpression:
                                        description: ByExpression is an expression
                                          that matches against the object for status
                                          matching
                                        properties:
                                          expectedResult:
                                            description: ExpectedResult is the expected
                                              result value in string format from the
                                              expression evaluation
                                            type: string
                                          expression:
                                            description: Expression is the CEL expression
                                              to evaluate.
                                            type: string
                                        required:
                                        - expectedResult
                                        - expression
                                        type: object
                                      byPhase:
                                        description: ByPhase matches against a specific
                                          phase value
                                        type: string
                                    type: object
                                  type: array
                                  x-kubernetes-list-type: atomic
                                degraded:
                                  description: |-
                                    Degraded defines matchers for the Degraded status.
                                    Multiple matchers are OR'd together.
                                  items:
                                    description: |-
                                      StatusMatcher defines criteria for matching a specific status.
                                      If both ByPhase and ByConditions are provided, ALL must match (AND logic).
                                    properties:
                                      byConditions:
                                        description: ByConditions matches against
                                          specific condition combinations (ANDed together)
                                          at least one of status or reason must be
                                          used.
                                        items:
                                          description: ExpectedCondition defines a
                                            condition type and status that must be
                                            present.
                                          properties:
                                            reason:
                                              description: Reason is the expected
                                                condition reason
                                              type: string
                                            status:
                                              description: Status is the expected
                                                condition status
                                              type: string
                                            type:
                                              description: Type is the condition type
                                                to match
                                              type: string
                                          required:
                                          - type
                                          type: object
                                        type: array
                                        x-kubernetes-list-type: atomic
                                      byExpression:
                                        description: ByExpression is an expression
                                          that matches against the object for status
                                          matching
                                        properties:
                                          expectedResult:
                                            description: ExpectedResult is the expected
                                              result value in string format from the
                                              expression evaluation
                                            type: string
                                          expression:
                                            description: Expression is the CEL expression
                                              to evaluate.
                                            type: string
                                        required:
                                        - expectedResult
                                        - expression
                                        type: object
                                      byPhase:
                                        description: ByPhase matches against a specific
                                          phase value
                                        type: string
                                    type: object
                                  type: array
                                  x-kubernetes-list-type: atomic
                                failed:
                                  description: |-
                                    Failed defines matchers for the Failed status.
                                    Multiple matchers are OR'd together.
                                  items:
                                    description: |-
                                      StatusMatcher defines criteria for matching a specific status.
                                      If both ByPhase and ByConditions are provided, ALL must match (AND logic).
                                    properties:
                                      byConditions:
                                        description: ByConditions matches against
                                          specific condition combinations (ANDed together)
                                          at least one of status or reason must be
                                          used.
                                        items:
                                          description: ExpectedCondition defines a
                                            condition type and status that must be
                                            present.
                                          properties:
                                            reason:
                                              description: Reason is the expected
                                                condition reason
                                              type: string
                                            status:
                                              description: Status is the expected
                                                condition status
                                              type: string
                                            type:
                                              description: Type is the condition type
                                                to match
                                              type: string
                                          required:
                                          - type
                                          type: object
                                        type: array
                                        x-kubernetes-list-type: atomic
                                      byExpression:
                                        description: ByExpression is an expression
                                          that matches against the object for status
                                          matching
                                        properties:
                                          expectedResult:
                                            description: ExpectedResult is the expected
                                              result value in string format from the
                                              expression evaluation
                                            type: string
                                          expression:
                                            description: Expression is the CEL expression
                                              to evaluate.
                                            type: string
                                        required:
                                        - expectedResult
                                        - expression
                                        type: object
                                      byPhase:
                                        description: ByPhase matches against a specific
                                          phase value
                                        type: string
                                    type: object
                                  type: array
                                  x-kubernetes-list-type: atomic
                                initializing:
                                  description: |-
                                    Initializing defines matchers for the Initializing status.
                                    Multiple matchers are OR'd together.
                                  items:
                                    description: |-
                                      StatusMatcher defines criteria for matching a specific status.
                                      If both ByPhase and ByConditions are provided, ALL must match (AND logic).
                                    properties:
                                      byConditions:
                                        description: ByConditions matches against
                                          specific condition combinations (ANDed together)
                                          at least one of status or reason must be
                                          used.
                                        items:
                                          description: ExpectedCondition defines a
                                            condition type and status that must be
                                            present.
                                          properties:
                                            reason:
                                              description: Reason is the expected
                                                condition reason
                                              type: string
                                            status:
                                              description: Status is the expected
                                                condition status
                                              type: string
                                            type:
                                              description: Type is the condition type
                                                to match
                                              type: string
                                          required:
                                          - type
                                          type: object
                                        type: array
                                        x-kubernetes-list-type: atomic
                                      byExpression:
                                        description: ByExpression is an expression
                                          that matches against the object for status
                                          matching
                                        properties:
                                          expectedResult:
                                            description: ExpectedResult is the expected
                                              result value in string format from the
                                              expression evaluation
                                            type: string
                                          expression:
                                            description: Expression is the CEL expression
                                              to evaluate.
                                            type: string
                                        required:
                                        - expectedResult
                                        - expression
                                        type: object
                                      byPhase:
                                        description: ByPhase matches against a specific
                                          phase value
                                        type: string
                                    type: object
                                  type: array
                                  x-kubernetes-list-type: atomic
                                resuming:
                                  description: |-
                                    Resuming defines matchers for the Resuming status
                                    Multiple matchers are OR'd together.
                                  items:
                                    description: |-
                                      StatusMatcher defines criteria for matching a specific status.
                                      If both ByPhase and ByConditions are provided, ALL must match (AND logic).
                                    properties:
                                      byConditions:
                                        description: ByConditions matches against
                                          specific condition combinations (ANDed together)
                                          at least one of status or reason must be
                                          used.
                                        items:
                                          description: ExpectedCondition defines a
                                            condition type and status that must be
                                            present.
                                          properties:
                                            reason:
                                              description: Reason is the expected
                                                condition reason
                                              type: string
                                            status:
                                              description: Status is the expected
                                                condition status
                                              type: string
                                            type:
                                              description: Type is the condition type
                                                to match
                                              type: string
                                          required:
                                          - type
                                          type: object
                                        type: array
                                        x-kubernetes-list-type: atomic
                                      byExpression:
                                        description: ByExpression is an expression
                                          that matches against the object for status
                                          matching
                                        properties:
                                          expectedResult:
                                            description: ExpectedResult is the expected
                                              result value in string format from the
                                              expression evaluation
                                            type: string
                                          expression:
                                            description: Expression is the CEL expression
                                              to evaluate.
                                            type: string
                                        required:
                                        - expectedResult
                                        - expression
                                        type: object
                                      byPhase:
                                        description: ByPhase matches against a specific
                                          phase value
                                        type: string
                                    type: object
                                  type: array
                                  x-kubernetes-list-type: atomic
                                running:
                                  description: |-
                                    Running defines matchers for the Running status.
                                    Multiple matchers are OR'd together.
                                  items:
                                    description: |-
                                      StatusMatcher defines criteria for matching a specific status.
                                      If both ByPhase and ByConditions are provided, ALL must match (AND logic).
                                    properties:
                                      byConditions:
                                        description: ByConditions matches against
                                          specific condition combinations (ANDed together)
                                          at least one of status or reason must be
                                          used.
                                        items:
                                          description: ExpectedCondition defines a
                                            condition type and status that must be
                                            present.
                                          properties:
                                            reason:
                                              description: Reason is the expected
                                                condition reason
                                              type: string
                                            status:
                                              description: Status is the expected
                                                condition status
                                              type: string
                                            type:
                                              description: Type is the condition type
                                                to match
                                              type: string
                                          required:
                                          - type
                                          type: object
                                        type: array
                                        x-kubernetes-list-type: atomic
                                      byExpression:
                                        description: ByExpression is an expression
                                          that matches against the object for status
                                          matching
                                        properties:
                                          expectedResult:
                                            description: ExpectedResult is the expected
                                              result value in string format from the
                                              expression evaluation
                                            type: string
                                          expression:
                                            description: Expression is the CEL expression
                                              to evaluate.
                                            type: string
                                        required:
                                        - expectedResult
                                        - expression
                                        type: object
                                      byPhase:
                                        description: ByPhase matches against a specific
                                          phase value
                                        type: string
                                    type: object
                                  type: array
                                  x-kubernetes-list-type: atomic
                                suspended:
                                  description: |-
                                    Suspended defines matchers for the Suspended status
                                    Multiple matchers are OR'd together.
                                  items:
                                    description: |-
                                      StatusMatcher defines criteria for matching a specific status.
                                      If both ByPhase and ByConditions are provided, ALL must match (AND logic).
                                    properties:
                                      byConditions:
                                        description: ByConditions matches against
                                          specific condition combinations (ANDed together)
                                          at least one of status or reason must be
                                          used.
                                        items:
                                          description: ExpectedCondition defines a
                                            condition type and status that must be
                                            present.
                                          properties:
                                            reason:
                                              description: Reason is the expected
                                                condition reason
                                              type: string
                                            status:
                                              description: Status is the expected
                                                condition status
                                              type: string
                                            type:
                                              description: Type is the condition type
                                                to match
                                              type: string
                                          required:
                                          - type
                                          type: object
                                        type: array
                                        x-kubernetes-list-type: atomic
                                      byExpression:
                                        description: ByExpression is an expression
                                          that matches against the object for status
                                          matching
                                        properties:
                                          expectedResult:
                                            description: ExpectedResult is the expected
                                              result value in string format from the
                                              expression evaluation
                                            type: string
                                          expression:
                                            description: Expression is the CEL expression
                                              to evaluate.
                                            type: string
                                        required:
                                        - expectedResult
                                        - expression
                                        type: object
                                      byPhase:
                                        description: ByPhase matches against a specific
                                          phase value
                                        type: string
                                    type: object
                                  type: array
                                  x-kubernetes-list-type: atomic
                                suspending:
                                  description: |-
                                    Suspending defines matchers for the Suspending status
                                    Multiple matchers are OR'd together.
                                  items:
                                    description: |-
                                      StatusMatcher defines criteria for matching a specific status.
                                      If both ByPhase and ByConditions are provided, ALL must match (AND logic).
                                    properties:
                                      byConditions:
                                        description: ByConditions matches against
                                          specific condition combinations (ANDed together)
                                          at least one of status or reason must be
                                          used.
                                        items:
                                          description: ExpectedCondition defines a
                                            condition type and status that must be
                                            present.
                                          properties:
                                            reason:
                                              description: Reason is the expected
                                                condition reason
                                              type: string
                                            status:
                                              description: Status is the expected
                                                condition status
                                              type: string
                                            type:
                                              description: Type is the condition type
                                                to match
                                              type: string
                                          required:
                                          - type
                                          type: object
                                        type: array
                                        x-kubernetes-list-type: atomic
                                      byExpression:
                                        description: ByExpression is an expression
                                          that matches against the object for status
                                          matching
                                        properties:
                                          expectedResult:
                                            description: ExpectedResult is the expected
                                              result value in string format from the
                                              expression evaluation
                                            type: string
                                          expression:
                                            description: Expression is the CEL expression
                                              to evaluate.
                                            type: string
                                        required:
                                        - expectedResult
                                        - expression
                                        type: object
                                      byPhase:
                                        description: ByPhase matches against a specific
                                          phase value
                                        type: string
                                    type: object
                                  type: array
                                  x-kubernetes-list-type: atomic
                              type: object
                          required:
                          - statusMappings
                          type: object
                        suspendDefinition:
                          description: |-
                            SuspendDefinition locates the boolean field used to suspend and resume this component.
                            Should only be populated for components where the underlying framework provides native, first-class support for suspension primitives.
                            (e.g. spec.suspend for batch/v1 Job).
                          properties:
                            pathWrite:
                              description: PathWrite is the field's RFC 6901 JSON
                                Pointer. An empty string targets the document root.
                              pattern: ^(/([^~/]|~[01])*)*$
                              type: string
                            pathWriteExpression:
                              description: PathWriteExpression returns the field's
                                RFC 6901 JSON Pointer using the definition's engine.
                              minLength: 1
                              type: string
                          type: object
                          x-kubernetes-validations:
                          - message: exactly one of pathWrite or pathWriteExpression
                              must be set
                            rule: has(self.pathWrite) != has(self.pathWriteExpression)
                      required:
                      - name
                      type: object
                      x-kubernetes-validations:
                      - message: field names must not be empty or reserved for a built-in
                          SDK field
                        rule: '!has(self.fields) || (!("" in self.fields) && !(''podTemplateSpec''
                          in self.fields) && !(''podSpec'' in self.fields) && !(''metadata''
                          in self.fields) && !(''fragmented.schedulerName'' in self.fields)
                          && !(''fragmented.labels'' in self.fields))'
                      - message: field names must not be reserved for a built-in SDK
                          field
                        rule: '!has(self.fields) || (!(''fragmented.annotations''
                          in self.fields) && !(''fragmented.resources'' in self.fields)
                          && !(''fragmented.resourceClaims'' in self.fields) && !(''fragmented.podAffinity''
                          in self.fields) && !(''fragmented.nodeAffinity'' in self.fields)
                          && !(''fragmented.containers'' in self.fields))'
                      - message: field names must not be reserved for a built-in SDK
                          field
                        rule: '!has(self.fields) || (!(''fragmented.container'' in
                          self.fields) && !(''fragmented.priorityClassName'' in self.fields)
                          && !(''fragmented.image'' in self.fields) && !(''scale.replicas''
                          in self.fields) && !(''scale.minReplicas'' in self.fields)
                          && !(''scale.maxReplicas'' in self.fields))'
                      - message: suspend is a reserved built-in SDK field
                        rule: '!has(self.fields) || !(''suspend'' in self.fields)'
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  references:
                    description: |-
                      References declares cluster resources whose values are exposed to the
                      workload's expressions as references.<name>. Karta fetches nothing itself:
                      the consumer resolves each reference and passes the values in.
                    items:
                      description: "ResourceReference declares another resource (or
                        set of resources) whose values are\nexposed to every component's
                        expressions as references.<name>.\nExactly one of Lookup or
                        List is set:\n\n\tLookup -> references.<name> is a single
                        object (absent when not found)\n\tList   -> references.<name>
                        is a list (possibly empty)\n\nA namespaced reference always
                        resolves in the workload's own namespace; there is\ndeliberately
                        no namespace field."
                      properties:
                        gvk:
                          description: GVK is the group/version/kind of the referenced
                            resource(s).
                          properties:
                            group:
                              description: Group is the API group of the resource
                              type: string
                            kind:
                              description: Kind is the API kind of the resource
                              type: string
                            version:
                              description: Version is the API version of the resource
                              type: string
                          required:
                          - group
                          - kind
                          - version
                          type: object
                        list:
                          description: List fetches a set of objects by a structured
                            label selector.
                          properties:
                            matchExpressions:
                              description: MatchExpressions selects resources by label
                                selector requirements.
                              items:
                                description: |-
                                  LabelSelectorRequirement mirrors the Kubernetes selector requirement, except its
                                  values may be sourced from the root object.
                                properties:
                                  key:
                                    description: Key is the label key the requirement
                                      applies to.
                                    minLength: 1
                                    type: string
                                  operator:
                                    description: Operator is the requirement's relationship
                                      to its values.
                                    enum:
                                    - In
                                    - NotIn
                                    - Exists
                                    - DoesNotExist
                                    type: string
                                  values:
                                    description: Values are required for In/NotIn
                                      and must be empty for Exists/DoesNotExist.
                                    items:
                                      description: |-
                                        LabelValue is a single label value: either a literal (Value) or a CEL expression
                                        evaluated against the root object (Expression). Exactly one is set.
                                      properties:
                                        expression:
                                          description: Expression is a CEL expression
                                            against the root object resolving the
                                            value.
                                          type: string
                                        value:
                                          description: Value is a literal label value.
                                          type: string
                                      type: object
                                      x-kubernetes-validations:
                                      - message: exactly one of value or expression
                                          must be set
                                        rule: has(self.value) != has(self.expression)
                                    type: array
                                    x-kubernetes-list-type: atomic
                                required:
                                - key
                                - operator
                                type: object
                              type: array
                              x-kubernetes-list-type: atomic
                            matchLabels:
                              additionalProperties:
                                description: |-
                                  LabelValue is a single label value: either a literal (Value) or a CEL expression
                                  evaluated against the root object (Expression). Exactly one is set.
                                properties:
                                  expression:
                                    description: Expression is a CEL expression against
                                      the root object resolving the value.
                                    type: string
                                  value:
                                    description: Value is a literal label value.
                                    type: string
                                type: object
                                x-kubernetes-validations:
                                - message: exactly one of value or expression must
                                    be set
                                  rule: has(self.value) != has(self.expression)
                              description: MatchLabels selects resources whose labels
                                equal each resolved value.
                              type: object
                          type: object
                        lookup:
                          description: Lookup fetches a single object by name.
                          properties:
                            nameExpression:
                              description: |-
                                NameExpression is a CEL expression against the root object resolving the
                                referenced resource's name.
                              minLength: 1
                              type: string
                          required:
                          - nameExpression
                          type: object
                        name:
                          description: Name is the variable name the reference is
                            exposed under, as references.<name>.
                          minLength: 1
                          type: string
                      required:
                      - gvk
                      - name
                      type: object
                      x-kubernetes-validations:
                      - message: exactly one of lookup or list must be set
                        rule: has(self.lookup) != has(self.list)
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  rootComponent:
                    description: RootComponent defines the top-level component of
                      the workload hierarchy
                    properties:
                      fields:
                        additionalProperties:
                          description: |-
                            ValueAccessor reads and locates one workload field.
                            Expression is evaluated by the definition's engine and returns the field's value.
                            PathWrite or PathWriteExpression locates the field for writes. An accessor without
                            either is read-only. The SDK owns patch construction and write strategy.
                          properties:
                            expression:
                              description: Expression returns the field's value.
                              type: string
                            pathWrite:
                              description: PathWrite is the field's RFC 6901 JSON
                                Pointer. An empty string targets the document root.
                              pattern: ^(/([^~/]|~[01])*)*$
                              type: string
                            pathWriteExpression:
                              description: PathWriteExpression returns the field's
                                RFC 6901 JSON Pointer using the definition's engine.
                              minLength: 1
                              type: string
                          type: object
                          x-kubernetes-validations:
                          - message: pathWrite and pathWriteExpression are mutually
                              exclusive
                            rule: '!(has(self.pathWrite) && has(self.pathWriteExpression))'
                        description: |-
                          Fields exposes custom named workload values through the SDK's field accessors.
                          Built-in SDK field names are reserved.
                        type: object
                      instanceIds:
                        description: InstanceIds is an expression returning the list
                          of instance ids.
                        properties:
                          expression:
                            description: Expression returns the field's value.
                            type: string
                          pathWrite:
                            description: PathWrite is the field's RFC 6901 JSON Pointer.
                              An empty string targets the document root.
                            pattern: ^(/([^~/]|~[01])*)*$
                            type: string
                          pathWriteExpression:
                            description: PathWriteExpression returns the field's RFC
                              6901 JSON Pointer using the definition's engine.
                            minLength: 1
                            type: string
                        type: object
                        x-kubernetes-validations:
                        - message: instanceIds is read-only
                          rule: '!has(self.pathWrite) && !has(self.pathWriteExpression)'
                        - message: pathWrite and pathWriteExpression are mutually
                            exclusive
                          rule: '!(has(self.pathWrite) && has(self.pathWriteExpression))'
                      kind:
                        description: Kind specifies the Kubernetes GroupVersionKind
                          for this component
                        properties:
                          group:
                            description: Group is the API group of the resource
                            type: string
                          kind:
                            description: Kind is the API kind of the resource
                            type: string
                          version:
                            description: Version is the API version of the resource
                            type: string
                        required:
                        - group
                        - kind
                        - version
                        type: object
                      name:
                        description: Name is the unique identifier for this component
                          within the Karta
                        type: string
                      ownerRef:
                        description: OwnerRef references the parent component in the
                          hierarchy by name (nil for root component)
                        type: string
                      podSelector:
                        description: PodSelector defines how to identify pods belonging
                          to this component
                        properties:
                          componentInstanceSelector:
                            description: |-
                              ComponentInstanceSelector splits a single ComponentDefinition into multiple
                              component instances, each identified by a unique value extracted from the pod.
                              Use this when instances of the same component type represent fundamentally
                              different roles or services (e.g., Dynamo "service" definition produces
                              separate "Frontend", "PrefillWorker", "DecodeWorker" instances).
                              This is distinct from ReplicaSelector: ComponentInstanceSelector creates
                              instances with potentially different specs/behavior, while ReplicaSelector
                              creates replicas of an identical sub-structure.
                            properties:
                              expression:
                                description: Expression reads and writes this field.
                                type: string
                            type: object
                          componentTypeSelector:
                            description: |-
                              ComponentTypeSelector determines whether a pod belongs to this component type
                              by matching a pod label or annotation value via an expression.
                              This is the primary mechanism for pod-to-component membership.
                              For example, LWS "leader" uses worker-index="0" to identify leader pods,
                              and LWS "worker" checks for the existence of the leader-name annotation.
                            properties:
                              expression:
                                description: Expression reads and writes this field.
                                type: string
                              value:
                                description: Value is the expected value for the key
                                  (optional - if nil, only key existence is checked)
                                type: string
                            type: object
                          replicaSelector:
                            description: |-
                              ReplicaSelector identifies which replica index or group the pod belongs to.
                              Use this to distinguish between multiple replicas of the same component / component instance
                              sub-structure (e.g., LWS Group 0 vs Group 1, where each group has identical
                              leader/worker descendants). Descendant components automatically inherit the replica
                              context from their ancestor (parent), so ReplicaSelector should only be defined at
                              the level where replicas are created, not repeated in descendants.
                            properties:
                              expression:
                                description: Expression reads and writes this field.
                                type: string
                            type: object
                        type: object
                      scaleDefinition:
                        description: ScaleDefinition defines how to extract scaling
                          information from this component
                        properties:
                          maxReplicas:
                            description: MaxReplicas reads and writes this field.
                            properties:
                              expression:
                                description: Expression returns the field's value.
                                type: string
                              pathWrite:
                                description: PathWrite is the field's RFC 6901 JSON
                                  Pointer. An empty string targets the document root.
                                pattern: ^(/([^~/]|~[01])*)*$
                                type: string
                              pathWriteExpression:
                                description: PathWriteExpression returns the field's
                                  RFC 6901 JSON Pointer using the definition's engine.
                                minLength: 1
                                type: string
                            type: object
                            x-kubernetes-validations:
                            - message: pathWrite and pathWriteExpression are mutually
                                exclusive
                              rule: '!(has(self.pathWrite) && has(self.pathWriteExpression))'
                          minReplicas:
                            description: MinReplicas reads and writes this field.
                            properties:
                              expression:
                                description: Expression returns the field's value.
                                type: string
                              pathWrite:
                                description: PathWrite is the field's RFC 6901 JSON
                                  Pointer. An empty string targets the document root.
                                pattern: ^(/([^~/]|~[01])*)*$
                                type: string
                              pathWriteExpression:
                                description: PathWriteExpression returns the field's
                                  RFC 6901 JSON Pointer using the definition's engine.
                                minLength: 1
                                type: string
                            type: object
                            x-kubernetes-validations:
                            - message: pathWrite and pathWriteExpression are mutually
                                exclusive
                              rule: '!(has(self.pathWrite) && has(self.pathWriteExpression))'
                          replicas:
                            description: Replicas reads and writes this field.
                            properties:
                              expression:
                                description: Expression returns the field's value.
                                type: string
                              pathWrite:
                                description: PathWrite is the field's RFC 6901 JSON
                                  Pointer. An empty string targets the document root.
                                pattern: ^(/([^~/]|~[01])*)*$
                                type: string
                              pathWriteExpression:
                                description: PathWriteExpression returns the field's
                                  RFC 6901 JSON Pointer using the definition's engine.
                                minLength: 1
                                type: string
                            type: object
                            x-kubernetes-validations:
                            - message: pathWrite and pathWriteExpression are mutually
                                exclusive
                              rule: '!(has(self.pathWrite) && has(self.pathWriteExpression))'
                        type: object
                      specDefinition:
                        description: SpecDefinition defines how to extract pod specifications
                          from this component
                        properties:
                          fragmentedPodSpecDefinition:
                            description: |-
                              FragmentedPodSpecDefinition defines how to extract individual pod spec fields
                              when they are scattered across different paths in the component
                            properties:
                              annotations:
                                description: Annotations reads and writes this field.
                                properties:
                                  expression:
                                    description: Expression returns the field's value.
                                    type: string
                                  pathWrite:
                                    description: PathWrite is the field's RFC 6901
                                      JSON Pointer. An empty string targets the document
                                      root.
                                    pattern: ^(/([^~/]|~[01])*)*$
                                    type: string
                                  pathWriteExpression:
                                    description: PathWriteExpression returns the field's
                                      RFC 6901 JSON Pointer using the definition's
                                      engine.
                                    minLength: 1
                                    type: string
                                type: object
                                x-kubernetes-validations:
                                - message: pathWrite and pathWriteExpression are mutually
                                    exclusive
                                  rule: '!(has(self.pathWrite) && has(self.pathWriteExpression))'
                              container:
                                description: Container reads and writes this field.
                                properties:
                                  expression:
                                    description: Expression returns the field's value.
                                    type: string
                                  pathWrite:
                                    description: PathWrite is the field's RFC 6901
                                      JSON Pointer. An empty string targets the document
                                      root.
                                    pattern: ^(/([^~/]|~[01])*)*$
                                    type: string
                                  pathWriteExpression:
                                    description: PathWriteExpression returns the field's
                                      RFC 6901 JSON Pointer using the definition's
                                      engine.
                                    minLength: 1
                                    type: string
                                type: object
                                x-kubernetes-validations:
                                - message: pathWrite and pathWriteExpression are mutually
                                    exclusive
                                  rule: '!(has(self.pathWrite) && has(self.pathWriteExpression))'
                              containers:
                                description: Containers reads and writes this field.
                                properties:
                                  expression:
                                    description: Expression returns the field's value.
                                    type: string
                                  pathWrite:
                                    description: PathWrite is the field's RFC 6901
                                      JSON Pointer. An empty string targets the document
                                      root.
                                    pattern: ^(/([^~/]|~[01])*)*$
                                    type: string
                                  pathWriteExpression:
                                    description: PathWriteExpression returns the field's
                                      RFC 6901 JSON Pointer using the definition's
                                      engine.
                                    minLength: 1
                                    type: string
                                type: object
                                x-kubernetes-validations:
                                - message: pathWrite and pathWriteExpression are mutually
                                    exclusive
                                  rule: '!(has(self.pathWrite) && has(self.pathWriteExpression))'
                              image:
                                description: Image reads and writes this field.
                                properties:
                                  expression:
                                    description: Expression returns the field's value.
                                    type: string
                                  pathWrite:
                                    description: PathWrite is the field's RFC 6901
                                      JSON Pointer. An empty string targets the document
                                      root.
                                    pattern: ^(/([^~/]|~[01])*)*$
                                    type: string
                                  pathWriteExpression:
                                    description: PathWriteExpression returns the field's
                                      RFC 6901 JSON Pointer using the definition's
                                      engine.
                                    minLength: 1
                                    type: string
                                type: object
                                x-kubernetes-validations:
                                - message: pathWrite and pathWriteExpression are mutually
                                    exclusive
                                  rule: '!(has(self.pathWrite) && has(self.pathWriteExpression))'
                              labels:
                                description: Labels reads and writes this field.
                                properties:
                                  expression:
                                    description: Expression returns the field's value.
                                    type: string
                                  pathWrite:
                                    description: PathWrite is the field's RFC 6901
                                      JSON Pointer. An empty string targets the document
                                      root.
                                    pattern: ^(/([^~/]|~[01])*)*$
                                    type: string
                                  pathWriteExpression:
                                    description: PathWriteExpression returns the field's
                                      RFC 6901 JSON Pointer using the definition's
                                      engine.
                                    minLength: 1
                                    type: string
                                type: object
                                x-kubernetes-validations:
                                - message: pathWrite and pathWriteExpression are mutually
                                    exclusive
                                  rule: '!(has(self.pathWrite) && has(self.pathWriteExpression))'
                              nodeAffinity:
                                description: NodeAffinity reads and writes this field.
                                properties:
                                  expression:
                                    description: Expression returns the field's value.
                                    type: string
                                  pathWrite:
                                    description: PathWrite is the field's RFC 6901
                                      JSON Pointer. An empty string targets the document
                                      root.
                                    pattern: ^(/([^~/]|~[01])*)*$
                                    type: string
                                  pathWriteExpression:
                                    description: PathWriteExpression returns the field's
                                      RFC 6901 JSON Pointer using the definition's
                                      engine.
                                    minLength: 1
                                    type: string
                                type: object
                                x-kubernetes-validations:
                                - message: pathWrite and pathWriteExpression are mutually
                                    exclusive
                                  rule: '!(has(self.pathWrite) && has(self.pathWriteExpression))'
                              podAffinity:
                                description: PodAffinity reads and writes this field.
                                properties:
                                  expression:
                                    description: Expression returns the field's value.
                                    type: string
                                  pathWrite:
                                    description: PathWrite is the field's RFC 6901
                                      JSON Pointer. An empty string targets the document
                                      root.
                                    pattern: ^(/([^~/]|~[01])*)*$
                                    type: string
                                  pathWriteExpression:
                                    description: PathWriteExpression returns the field's
                                      RFC 6901 JSON Pointer using the definition's
                                      engine.
                                    minLength: 1
                                    type: string
                                type: object
                                x-kubernetes-validations:
                                - message: pathWrite and pathWriteExpression are mutually
                                    exclusive
                                  rule: '!(has(self.pathWrite) && has(self.pathWriteExpression))'
                              priorityClassName:
                                description: PriorityClassName reads and writes this
                                  field.
                                properties:
                                  expression:
                                    description: Expression returns the field's value.
                                    type: string
                                  pathWrite:
                                    description: PathWrite is the field's RFC 6901
                                      JSON Pointer. An empty string targets the document
                                      root.
                                    pattern: ^(/([^~/]|~[01])*)*$
                                    type: string
                                  pathWriteExpression:
                                    description: PathWriteExpression returns the field's
                                      RFC 6901 JSON Pointer using the definition's
                                      engine.
                                    minLength: 1
                                    type: string
                                type: object
                                x-kubernetes-validations:
                                - message: pathWrite and pathWriteExpression are mutually
                                    exclusive
                                  rule: '!(has(self.pathWrite) && has(self.pathWriteExpression))'
                              resourceClaims:
                                description: ResourceClaims reads and writes this
                                  field.
                                properties:
                                  expression:
                                    description: Expression returns the field's value.
                                    type: string
                                  pathWrite:
                                    description: PathWrite is the field's RFC 6901
                                      JSON Pointer. An empty string targets the document
                                      root.
                                    pattern: ^(/([^~/]|~[01])*)*$
                                    type: string
                                  pathWriteExpression:
                                    description: PathWriteExpression returns the field's
                                      RFC 6901 JSON Pointer using the definition's
                                      engine.
                                    minLength: 1
                                    type: string
                                type: object
                                x-kubernetes-validations:
                                - message: pathWrite and pathWriteExpression are mutually
                                    exclusive
                                  rule: '!(has(self.pathWrite) && has(self.pathWriteExpression))'
                              resources:
                                description: Resources reads and writes this field.
                                properties:
                                  expression:
                                    description: Expression returns the field's value.
                                    type: string
                                  pathWrite:
                                    description: PathWrite is the field's RFC 6901
                                      JSON Pointer. An empty string targets the document
                                      root.
                                    pattern: ^(/([^~/]|~[01])*)*$
                                    type: string
                                  pathWriteExpression:
                                    description: PathWriteExpression returns the field's
                                      RFC 6901 JSON Pointer using the definition's
                                      engine.
                                    minLength: 1
                                    type: string
                                type: object
                                x-kubernetes-validations:
                                - message: pathWrite and pathWriteExpression are mutually
                                    exclusive
                                  rule: '!(has(self.pathWrite) && has(self.pathWriteExpression))'
                              schedulerName:
                                description: SchedulerName reads and writes this field.
                                properties:
                                  expression:
                                    description: Expression returns the field's value.
                                    type: string
                                  pathWrite:
                                    description: PathWrite is the field's RFC 6901
                                      JSON Pointer. An empty string targets the document
                                      root.
                                    pattern: ^(/([^~/]|~[01])*)*$
                                    type: string
                                  pathWriteExpression:
                                    description: PathWriteExpression returns the field's
                                      RFC 6901 JSON Pointer using the definition's
                                      engine.
                                    minLength: 1
                                    type: string
                                type: object
                                x-kubernetes-validations:
                                - message: pathWrite and pathWriteExpression are mutually
                                    exclusive
                                  rule: '!(has(self.pathWrite) && has(self.pathWriteExpression))'
                            type: object
                          metadata:
                            description: Metadata reads and writes this field.
                            properties:
                              expression:
                                description: Expression returns the field's value.
                                type: string
                              pathWrite:
                                description: PathWrite is the field's RFC 6901 JSON
                                  Pointer. An empty string targets the document root.
                                pattern: ^(/([^~/]|~[01])*)*$
                                type: string
                              pathWriteExpression:
                                description: PathWriteExpression returns the field's
                                  RFC 6901 JSON Pointer using the definition's engine.
                                minLength: 1
                                type: string
                            type: object
                            x-kubernetes-validations:
                            - message: pathWrite and pathWriteExpression are mutually
                                exclusive
                              rule: '!(has(self.pathWrite) && has(self.pathWriteExpression))'
                          podSpec:
                            description: PodSpec reads and writes the complete PodSpec
                              object.
                            properties:
                              expression:
                                description: Expression returns the field's value.
                                type: string
                              pathWrite:
                                description: PathWrite is the field's RFC 6901 JSON
                                  Pointer. An empty string targets the document root.
                                pattern: ^(/([^~/]|~[01])*)*$
                                type: string
                              pathWriteExpression:
                                description: PathWriteExpression returns the field's
                                  RFC 6901 JSON Pointer using the definition's engine.
                                minLength: 1
                                type: string
                            type: object
                            x-kubernetes-validations:
                            - message: pathWrite and pathWriteExpression are mutually
                                exclusive
                              rule: '!(has(self.pathWrite) && has(self.pathWriteExpression))'
                          podTemplateSpec:
                            description: PodTemplateSpec reads and writes the complete
                              PodTemplateSpec object.
                            properties:
                              expression:
                                description: Expression returns the field's value.
                                type: string
                              pathWrite:
                                description: PathWrite is the field's RFC 6901 JSON
                                  Pointer. An empty string targets the document root.
                                pattern: ^(/([^~/]|~[01])*)*$
                                type: string
                              pathWriteExpression:
                                description: PathWriteExpression returns the field's
                                  RFC 6901 JSON Pointer using the definition's engine.
                                minLength: 1
                                type: string
                            type: object
                            x-kubernetes-validations:
                            - message: pathWrite and pathWriteExpression are mutually
                                exclusive
                              rule: '!(has(self.pathWrite) && has(self.pathWriteExpression))'
                        type: object
                      statusDefinition:
                        description: |-
                          StatusDefinition defines how to interpret the status of this component
                          should be added only for the root component
                        properties:
                          conditionsDefinition:
                            description: ConditionsDefinition defines how to extract
                              Kubernetes-style conditions
                            properties:
                              expression:
                                description: Expression reads and writes this field.
                                type: string
                              messageFieldName:
                                default: message
                                description: MessageFieldName is the field name for
                                  the condition text message
                                type: string
                              reasonFieldName:
                                default: reason
                                description: ReasonFieldName is the field name for
                                  the condition reason
                                type: string
                              statusFieldName:
                                default: status
                                description: StatusFieldName is the field name for
                                  the condition status
                                type: string
                              typeFieldName:
                                default: type
                                description: TypeFieldName is the field name for the
                                  condition type
                                type: string
                            type: object
                          phaseDefinition:
                            description: PhaseDefinition defines how to extract a
                              simple phase/state string
                            properties:
                              expression:
                                description: Expression reads and writes this field.
                                type: string
                            type: object
                          statusMappings:
                            description: StatusMappings define how to map extracted
                              status to ResourceStatus values
                            properties:
                              completed:
                                description: |-
                                  Completed defines matchers for the Completed status.
                                  Multiple matchers are OR'd together.
                                items:
                                  description: |-
                                    StatusMatcher defines criteria for matching a specific status.
                                    If both ByPhase and ByConditions are provided, ALL must match (AND logic).
                                  properties:
                                    byConditions:
                                      description: ByConditions matches against specific
                                        condition combinations (ANDed together) at
                                        least one of status or reason must be used.
                                      items:
                                        description: ExpectedCondition defines a condition
                                          type and status that must be present.
                                        properties:
                                          reason:
                                            description: Reason is the expected condition
                                              reason
                                            type: string
                                          status:
                                            description: Status is the expected condition
                                              status
                                            type: string
                                          type:
                                            description: Type is the condition type
                                              to match
                                            type: string
                                        required:
                                        - type
                                        type: object
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    byExpression:
                                      description: ByExpression is an expression that
                                        matches against the object for status matching
                                      properties:
                                        expectedResult:
                                          description: ExpectedResult is the expected
                                            result value in string format from the
                                            expression evaluation
                                          type: string
                                        expression:
                                          description: Expression is the CEL expression
                                            to evaluate.
                                          type: string
                                      required:
                                      - expectedResult
                                      - expression
                                      type: object
                                    byPhase:
                                      description: ByPhase matches against a specific
                                        phase value
                                      type: string
                                  type: object
                                type: array
                                x-kubernetes-list-type: atomic
                              degraded:
                                description: |-
                                  Degraded defines matchers for the Degraded status.
                                  Multiple matchers are OR'd together.
                                items:
                                  description: |-
                                    StatusMatcher defines criteria for matching a specific status.
                                    If both ByPhase and ByConditions are provided, ALL must match (AND logic).
                                  properties:
                                    byConditions:
                                      description: ByConditions matches against specific
                                        condition combinations (ANDed together) at
                                        least one of status or reason must be used.
                                      items:
                                        description: ExpectedCondition defines a condition
                                          type and status that must be present.
                                        properties:
                                          reason:
                                            description: Reason is the expected condition
                                              reason
                                            type: string
                                          status:
                                            description: Status is the expected condition
                                              status
                                            type: string
                                          type:
                                            description: Type is the condition type
                                              to match
                                            type: string
                                        required:
                                        - type
                                        type: object
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    byExpression:
                                      description: ByExpression is an expression that
                                        matches against the object for status matching
                                      properties:
                                        expectedResult:
                                          description: ExpectedResult is the expected
                                            result value in string format from the
                                            expression evaluation
                                          type: string
                                        expression:
                                          description: Expression is the CEL expression
                                            to evaluate.
                                          type: string
                                      required:
                                      - expectedResult
                                      - expression
                                      type: object
                                    byPhase:
                                      description: ByPhase matches against a specific
                                        phase value
                                      type: string
                                  type: object
                                type: array
                                x-kubernetes-list-type: atomic
                              failed:
                                description: |-
                                  Failed defines matchers for the Failed status.
                                  Multiple matchers are OR'd together.
                                items:
                                  description: |-
                                    StatusMatcher defines criteria for matching a specific status.
                                    If both ByPhase and ByConditions are provided, ALL must match (AND logic).
                                  properties:
                                    byConditions:
                                      description: ByConditions matches against specific
                                        condition combinations (ANDed together) at
                                        least one of status or reason must be used.
                                      items:
                                        description: ExpectedCondition defines a condition
                                          type and status that must be present.
                                        properties:
                                          reason:
                                            description: Reason is the expected condition
                                              reason
                                            type: string
                                          status:
                                            description: Status is the expected condition
                                              status
                                            type: string
                                          type:
                                            description: Type is the condition type
                                              to match
                                            type: string
                                        required:
                                        - type
                                        type: object
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    byExpression:
                                      description: ByExpression is an expression that
                                        matches against the object for status matching
                                      properties:
                                        expectedResult:
                                          description: ExpectedResult is the expected
                                            result value in string format from the
                                            expression evaluation
                                          type: string
                                        expression:
                                          description: Expression is the CEL expression
                                            to evaluate.
                                          type: string
                                      required:
                                      - expectedResult
                                      - expression
                                      type: object
                                    byPhase:
                                      description: ByPhase matches against a specific
                                        phase value
                                      type: string
                                  type: object
                                type: array
                                x-kubernetes-list-type: atomic
                              initializing:
                                description: |-
                                  Initializing defines matchers for the Initializing status.
                                  Multiple matchers are OR'd together.
                                items:
                                  description: |-
                                    StatusMatcher defines criteria for matching a specific status.
                                    If both ByPhase and ByConditions are provided, ALL must match (AND logic).
                                  properties:
                                    byConditions:
                                      description: ByConditions matches against specific
                                        condition combinations (ANDed together) at
                                        least one of status or reason must be used.
                                      items:
                                        description: ExpectedCondition defines a condition
                                          type and status that must be present.
                                        properties:
                                          reason:
                                            description: Reason is the expected condition
                                              reason
                                            type: string
                                          status:
                                            description: Status is the expected condition
                                              status
                                            type: string
                                          type:
                                            description: Type is the condition type
                                              to match
                                            type: string
                                        required:
                                        - type
                                        type: object
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    byExpression:
                                      description: ByExpression is an expression that
                                        matches against the object for status matching
                                      properties:
                                        expectedResult:
                                          description: ExpectedResult is the expected
                                            result value in string format from the
                                            expression evaluation
                                          type: string
                                        expression:
                                          description: Expression is the CEL expression
                                            to evaluate.
                                          type: string
                                      required:
                                      - expectedResult
                                      - expression
                                      type: object
                                    byPhase:
                                      description: ByPhase matches against a specific
                                        phase value
                                      type: string
                                  type: object
                                type: array
                                x-kubernetes-list-type: atomic
                              resuming:
                                description: |-
                                  Resuming defines matchers for the Resuming status
                                  Multiple matchers are OR'd together.
                                items:
                                  description: |-
                                    StatusMatcher defines criteria for matching a specific status.
                                    If both ByPhase and ByConditions are provided, ALL must match (AND logic).
                                  properties:
                                    byConditions:
                                      description: ByConditions matches against specific
                                        condition combinations (ANDed together) at
                                        least one of status or reason must be used.
                                      items:
                                        description: ExpectedCondition defines a condition
                                          type and status that must be present.
                                        properties:
                                          reason:
                                            description: Reason is the expected condition
                                              reason
                                            type: string
                                          status:
                                            description: Status is the expected condition
                                              status
                                            type: string
                                          type:
                                            description: Type is the condition type
                                              to match
                                            type: string
                                        required:
                                        - type
                                        type: object
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    byExpression:
                                      description: ByExpression is an expression that
                                        matches against the object for status matching
                                      properties:
                                        expectedResult:
                                          description: ExpectedResult is the expected
                                            result value in string format from the
                                            expression evaluation
                                          type: string
                                        expression:
                                          description: Expression is the CEL expression
                                            to evaluate.
                                          type: string
                                      required:
                                      - expectedResult
                                      - expression
                                      type: object
                                    byPhase:
                                      description: ByPhase matches against a specific
                                        phase value
                                      type: string
                                  type: object
                                type: array
                                x-kubernetes-list-type: atomic
                              running:
                                description: |-
                                  Running defines matchers for the Running status.
                                  Multiple matchers are OR'd together.
                                items:
                                  description: |-
                                    StatusMatcher defines criteria for matching a specific status.
                                    If both ByPhase and ByConditions are provided, ALL must match (AND logic).
                                  properties:
                                    byConditions:
                                      description: ByConditions matches against specific
                                        condition combinations (ANDed together) at
                                        least one of status or reason must be used.
                                      items:
                                        description: ExpectedCondition defines a condition
                                          type and status that must be present.
                                        properties:
                                          reason:
                                            description: Reason is the expected condition
                                              reason
                                            type: string
                                          status:
                                            description: Status is the expected condition
                                              status
                                            type: string
                                          type:
                                            description: Type is the condition type
                                              to match
                                            type: string
                                        required:
                                        - type
                                        type: object
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    byExpression:
                                      description: ByExpression is an expression that
                                        matches against the object for status matching
                                      properties:
                                        expectedResult:
                                          description: ExpectedResult is the expected
                                            result value in string format from the
                                            expression evaluation
                                          type: string
                                        expression:
                                          description: Expression is the CEL expression
                                            to evaluate.
                                          type: string
                                      required:
                                      - expectedResult
                                      - expression
                                      type: object
                                    byPhase:
                                      description: ByPhase matches against a specific
                                        phase value
                                      type: string
                                  type: object
                                type: array
                                x-kubernetes-list-type: atomic
                              suspended:
                                description: |-
                                  Suspended defines matchers for the Suspended status
                                  Multiple matchers are OR'd together.
                                items:
                                  description: |-
                                    StatusMatcher defines criteria for matching a specific status.
                                    If both ByPhase and ByConditions are provided, ALL must match (AND logic).
                                  properties:
                                    byConditions:
                                      description: ByConditions matches against specific
                                        condition combinations (ANDed together) at
                                        least one of status or reason must be used.
                                      items:
                                        description: ExpectedCondition defines a condition
                                          type and status that must be present.
                                        properties:
                                          reason:
                                            description: Reason is the expected condition
                                              reason
                                            type: string
                                          status:
                                            description: Status is the expected condition
                                              status
                                            type: string
                                          type:
                                            description: Type is the condition type
                                              to match
                                            type: string
                                        required:
                                        - type
                                        type: object
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    byExpression:
                                      description: ByExpression is an expression that
                                        matches against the object for status matching
                                      properties:
                                        expectedResult:
                                          description: ExpectedResult is the expected
                                            result value in string format from the
                                            expression evaluation
                                          type: string
                                        expression:
                                          description: Expression is the CEL expression
                                            to evaluate.
                                          type: string
                                      required:
                                      - expectedResult
                                      - expression
                                      type: object
                                    byPhase:
                                      description: ByPhase matches against a specific
                                        phase value
                                      type: string
                                  type: object
                                type: array
                                x-kubernetes-list-type: atomic
                              suspending:
                                description: |-
                                  Suspending defines matchers for the Suspending status
                                  Multiple matchers are OR'd together.
                                items:
                                  description: |-
                                    StatusMatcher defines criteria for matching a specific status.
                                    If both ByPhase and ByConditions are provided, ALL must match (AND logic).
                                  properties:
                                    byConditions:
                                      description: ByConditions matches against specific
                                        condition combinations (ANDed together) at
                                        least one of status or reason must be used.
                                      items:
                                        description: ExpectedCondition defines a condition
                                          type and status that must be present.
                                        properties:
                                          reason:
                                            description: Reason is the expected condition
                                              reason
                                            type: string
                                          status:
                                            description: Status is the expected condition
                                              status
                                            type: string
                                          type:
                                            description: Type is the condition type
                                              to match
                                            type: string
                                        required:
                                        - type
                                        type: object
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    byExpression:
                                      description: ByExpression is an expression that
                                        matches against the object for status matching
                                      properties:
                                        expectedResult:
                                          description: ExpectedResult is the expected
                                            result value in string format from the
                                            expression evaluation
                                          type: string
                                        expression:
                                          description: Expression is the CEL expression
                                            to evaluate.
                                          type: string
                                      required:
                                      - expectedResult
                                      - expression
                                      type: object
                                    byPhase:
                                      description: ByPhase matches against a specific
                                        phase value
                                      type: string
                                  type: object
                                type: array
                                x-kubernetes-list-type: atomic
                            type: object
                        required:
                        - statusMappings
                        type: object
                      suspendDefinition:
                        description: |-
                          SuspendDefinition locates the boolean field used to suspend and resume this component.
                          Should only be populated for components where the underlying framework provides native, first-class support for suspension primitives.
                          (e.g. spec.suspend for batch/v1 Job).
                        properties:
                          pathWrite:
                            description: PathWrite is the field's RFC 6901 JSON Pointer.
                              An empty string targets the document root.
                            pattern: ^(/([^~/]|~[01])*)*$
                            type: string
                          pathWriteExpression:
                            description: PathWriteExpression returns the field's RFC
                              6901 JSON Pointer using the definition's engine.
                            minLength: 1
                            type: string
                        type: object
                        x-kubernetes-validations:
                        - message: exactly one of pathWrite or pathWriteExpression
                            must be set
                          rule: has(self.pathWrite) != has(self.pathWriteExpression)
                    required:
                    - name
                    type: object
                    x-kubernetes-validations:
                    - message: field names must not be empty or reserved for a built-in
                        SDK field
                      rule: '!has(self.fields) || (!("" in self.fields) && !(''podTemplateSpec''
                        in self.fields) && !(''podSpec'' in self.fields) && !(''metadata''
                        in self.fields) && !(''fragmented.schedulerName'' in self.fields)
                        && !(''fragmented.labels'' in self.fields))'
                    - message: field names must not be reserved for a built-in SDK
                        field
                      rule: '!has(self.fields) || (!(''fragmented.annotations'' in
                        self.fields) && !(''fragmented.resources'' in self.fields)
                        && !(''fragmented.resourceClaims'' in self.fields) && !(''fragmented.podAffinity''
                        in self.fields) && !(''fragmented.nodeAffinity'' in self.fields)
                        && !(''fragmented.containers'' in self.fields))'
                    - message: field names must not be reserved for a built-in SDK
                        field
                      rule: '!has(self.fields) || (!(''fragmented.container'' in self.fields)
                        && !(''fragmented.priorityClassName'' in self.fields) && !(''fragmented.image''
                        in self.fields) && !(''scale.replicas'' in self.fields) &&
                        !(''scale.minReplicas'' in self.fields) && !(''scale.maxReplicas''
                        in self.fields))'
                    - message: suspend is a reserved built-in SDK field
                      rule: '!has(self.fields) || !(''suspend'' in self.fields)'
                required:
                - rootComponent
                type: object
              variables:
                description: |-
                  Variables are named expressions available to every expression in the definition as
                  variables.<name> - the composition mechanism a ValidatingAdmissionPolicy has, for breaking
                  a complex expression into named parts and evaluating a shared part once per read or write.
                  Variables are evaluated in order, and a later variable may reference an earlier one.
                items:
                  description: Variable is a named expression, the same shape a ValidatingAdmissionPolicy
                    variable has.
                  properties:
                    expression:
                      description: Expression computes the variable's value from the
                        workload.
                      type: string
                    name:
                      description: Name is how expressions reference this variable.
                      type: string
                  required:
                  - expression
                  - name
                  type: object
                type: array
                x-kubernetes-list-type: atomic
            required:
            - structureDefinition
            type: object
          status:
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
